"""mumbles - local, offline voice dictation.

Hold a hotkey, talk, and the transcript lands in whatever app has focus.
Audio never leaves the machine unless you explicitly turn on an LLM
formatting mode that uses a remote provider.
"""

__version__ = "0.1.0"
